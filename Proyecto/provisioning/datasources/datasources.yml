apiVersion: 1

datasources:
  - name: IOTSensors
    type: yesoreyeram-infinity-datasource
    access: proxy
    # IMPORTANTE: Cambia esta URL por la de tu servicio Flask en Render
    url: https://iotadvancedprgm.onrender.com
    isDefault: true
    editable: true
    version: 1
    uid: iot-sensors-infinity
    jsonData:
      # Configuraciones básicas del datasource Infinity
      tlsSkipVerify: false